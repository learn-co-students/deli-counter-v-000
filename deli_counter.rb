# Write your code here.
katz_deli = []

def line(katz_deli)
  if deli size == 0
    puts "The line is currently empty."
     array.each_with_index {|name, i| line_message << " #{i+1}. #{name}"}
  else
    puts "Welcome #{name}. You are #{number} in line."
  end
end

def take_a_number(katz_deli, customername)
  katz_deli = []
  aarray.push("#{customername}")
  puts "Welcome #{customername}, you are number #{array.size} in line."
end

def now_serving(katz_deli)
  if array.size > 0
  puts "Currently serving #{array.size + 1}."
else
  puts "There is nobody waiting to be served."
 end
end
